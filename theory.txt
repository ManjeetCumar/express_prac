project structure
   xyz
      server.js
      pakage.json
      /config - config.json (keys etc) 
      /models
      /routes - routes.js
      /src - source code in readable/writable form
      /dist - distributable ( contains files that can be directly used by others - minified/concatenated
              version actually used on production sites)   


 express:
    framework of node.     